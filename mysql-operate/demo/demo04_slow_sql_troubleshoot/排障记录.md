# SQL慢查询排障优化记录

## 1. 建表，导入数据。

表结构：
```
CREATE TABLE `tb_sku` (
  `id` int(11) NOT NULL AUTO_INCREMENT COMMENT '商品id',
  `sn` varchar(100) NOT NULL COMMENT '商品条码',
  `name` varchar(200) NOT NULL COMMENT 'SKU名称',
  `price` int(20) NOT NULL COMMENT '价格（分）',
  `num` int(10) NOT NULL COMMENT '库存数量',
  `alert_num` int(11) DEFAULT NULL COMMENT '库存预警数量',
  `image` varchar(200) DEFAULT NULL COMMENT '商品图片',
  `images` varchar(2000) DEFAULT NULL COMMENT '商品图片列表',
  `weight` int(11) DEFAULT NULL COMMENT '重量（克）',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `category_name` varchar(200) DEFAULT NULL COMMENT '类目名称',
  `brand_name` varchar(100) DEFAULT NULL COMMENT '品牌名称',
  `spec` varchar(200) DEFAULT NULL COMMENT '规格',
  `sale_num` int(11) DEFAULT '0' COMMENT '销量',
  `comment_num` int(11) DEFAULT '0' COMMENT '评论数',
  `status` char(1) DEFAULT '1' COMMENT '商品状态 1-正常，2-下架，3-删除',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```
登录客户端
```
mysql --local-infile -u root -p
```
导入数据
```
set global local_infile=1;
load data local infile '/home/jonathanai/test/tb_sku1.sql' into table `tb_sku` fields terminated by ',' lines terminated by '\n';
```

> **注：该文件共有200万行数据**

追踪慢查询日志
```
tail -f /var/lib/mysql/192-slow.log
```

> 注：long_query_time=0.8，即执行超过0.8秒的查询语句将被记录进日志。

## 2. 查询数据，追踪、优化慢查询

慢查询语句1（经日志捕捉到执行时间为1.07s)：查询指定品牌、品类的上架商品的ID、名字和销量

```
SELECT id,name,sale_num FROM tb_sku 
WHERE category_name = '牛仔裤' AND status = '1' AND brand_name='ME&CITY' 
ORDER BY id;

```
原因分析：WHERE 子句中三个筛选字段均无索引，系统只能执行全表扫描，逐行读取完整数据表校验筛选条件，百万级数据下 IO 开销巨大。
优化办法：建立包含category_name, status, brand_name的联合索引。
```
create index idx_sku_cate_sta_bra 
on tb_sku(category_name,status,brand_name);
```
优化效果：
字段|优化前|优化后|性能解读
--|--|--|--
select_type|SIMPLE|SIMPLE|单表普通查询，无关联子查询，无变化
table|tb_sku|tb_sku | 操作同一张商品表
type|index|ref | 巨大提升，优化前 type=index 遍历整棵主键索引树，优化后 ref 等值精准匹配索引
possible_keys|NULL|idx_sku_cate_sta_bra | 优化前无可用业务索引，优化后匹配自建联合索引
key|PRIMARY|idx_sku_cate_sta_bra | 优化前仅能遍历主键索引，优化后命中业务联合索引
key_len|4|1211|key_len 为索引占用字节，联合索引覆盖多筛选字段
ref|NULL|const,const,const | 优化前无常量匹配，优化后 3 个等值条件全部常量精准匹配
rows|1802083|2029 | 预估扫描行数从 180 万骤降至 2029，IO 读取大幅减少
filtered|0.10|100.00 | 优化前仅 0.1% 有效数据，优化后索引直接筛选全部符合条件数据
Extra|Using where|Using index condition | 优化前服务层过滤数据，优化后 ICP 索引下推减少回表 IO
实测执行耗时 |1.07s|0.01s | 整体查询性能大幅提升

验证：
- 优化后重新执行该 SQL，返回结果与优化前一致。
- 使用覆盖索引可进一步避免回表，综合执行优化时间与索引所占空间考虑，本次未使用该优化方法。

#
慢查询语句2（经日志捕捉到执行时间1.43s）：按种类和品牌名分组计数。
```
select category_name,brand_name,count(*) from tb_sku 
group by category_name,brand_name;
```
原因分析：分组字段顺序不符合语句1优化后创建的索引顺序，导致部分索引失效。
优化办法：建立包含category_name,  brand_name的联合索引。
```
create index idx_sku_cate_bra 
on tb_sku(category_name,brand_name);
```
优化效果：
字段|优化前|优化后|性能解读
--|--|--|--
select_type|SIMPLE|SIMPLE|单表分组查询，无联表、子查询，无变化 table|tb_sku|tb_sku|操作同一张商品SKU表 
type|index|index|均走索引扫描，但索引有序性差异导致处理成本天差地别
key|idx_sku_cate_sta_bra|idx_cate_bra|优化前使用字段顺序与group by不匹配的索引；优化后命中和分组字段完全匹配的联合索引 
key_len|1211|1206|均为长多字段复合索引，优化后索引字段更少，占用空间略小 
ref|NULL|NULL|无等值筛选条件，分组场景无常量索引匹配 
rows|1902083|1902083|预估索引扫描总行数相同，但分组运算开销差距巨大 
filtered|100.00|100.00|全量索引数据参与分组统计，不存在过滤逻辑 
Extra|Using index; Using temporary; Using filesort|Using index|优化前需临时表存储分组数据+文件排序，内存磁盘消耗极高；优化后覆盖索引天然有序，直接完成分组，无额外排序与临时表开销 
实测执行耗时|1.43s|0.40s|消除临时表、文件排序两大性能瓶颈，查询效率大幅提升

验证：

-   优化后重新执行该 SQL，返回结果与优化前一致。
-   针对1、2语句优化的两个索引可合并为字段顺序为category_name,  brand_name,status的联合索引，进一步节省空间。

#
慢查询语句3（经日志捕捉到执行时间1.00s）：
```
select count(*) from tb_sku where date(create_time)='2019-05-01';
```
原因分析：分组字段顺序不符合语句1优化后创建的索引顺序，导致部分索引失效。  
优化办法：建立create_time的单列索引。
优化效果：
字段|优化前|优化后|性能解读
--|--|--|--
select_type|SIMPLE|SIMPLE | 单表聚合查询，无联表子查询，无变化
table|tb_sku|tb_sku | 操作同一张商品 SKU 表
type|ALL|index | 显著提升，优化前 type=ALL 全表扫描，遍历整表所有数据行；优化后 type=index 全索引扫描，仅遍历索引树
possible_keys|NULL|NULL | 两者 possible_keys 均为 NULL，因 date () 函数对索引列做运算导致索引失效，无法走范围查询
key|NULL|idx_sku_cre | 优化前无索引可用；优化后命中时间字段单列索引，走覆盖索引扫描
key_len|NULL|6 | 优化后索引占用 6 字节，为 datetime 类型固定长度
ref|NULL|NULL | 无等值匹配条件，均为 NULL
rows|1902083|1902083 | 预估扫描行数量级相同，但扫描对象不同：全表数据行 vs 索引条目，索引体积远小于数据行
filtered|100.00|100.00 | 均需全量扫描后过滤，过滤率一致
Extra|Using where|Using where; Using index | 优化前全表扫描后服务层过滤，大量磁盘 IO；优化后覆盖索引扫描，无需回表读取数据行，IO 开销大幅降低
实测执行耗时 | 1.00s | 0.42s | 从全表扫描升级为覆盖索引扫描，避免读取大量数据页，性能显著提升
#

慢查询语句4（经日志捕捉到执行时间3.73s）：查询商品种类前两字为“牛仔”的所有商品信息。
```
SELECT * FROM tb_sku WHERE LEFT(category_name, 2) = '牛仔';
```
原因分析：对category_name进行函数运算，导致索引失效。
优化办法：将截断字符串改为模糊匹配
```
SELECT * FROM tb_sku WHERE category_name LIKE '牛仔%';
```
优化效果：
字段|优化前|优化后|性能解读
--|--|--|--
select_type|SIMPLE|SIMPLE | 单表普通查询，无联表子查询，无变化
table|tb_sku|tb_sku | 操作同一张商品 SKU 表
type|index|range | 巨大提升，优化前 type=index 全索引扫描；优化后 type=range 索引范围扫描，仅扫描匹配区间
possible_keys|NULL|idx_sku_cate_name | 优化前左模糊 % 开头导致索引失效，无可用索引；优化后前缀匹配可走多个候选索引
key|idx_sku_cate_name|idx_sku_cate_name | 都命中分类名称索引，但前者是被迫全扫，后者是有效范围查询
key_len|1805|803 | 优化前索引字节更长，需完整读取整列；优化后仅读取匹配前缀，索引效率更高
ref|NULL|NULL | 范围查询场景无常量等值匹配
rows|1902083|844696 | 预估扫描行数从 190 万降至 84 万，IO 读取量减少约 55%
filtered|100.00|100.00 | 均为索引直接筛选，过滤率一致
Extra|Using where; Using index|Using where; Using index | 均为覆盖索引无需回表，但优化前需遍历全部索引行再过滤；优化后直接定位索引区间
实测执行耗时 | 3.73s | 2.78s | 消除全索引扫描，走范围查询，性能提升较大

验证：
-   优化后重新执行该 SQL，返回结果与优化前一致。
-   优化后结果耗时仍较长，原因是查阅数据为整行，没有覆盖索引，仍有优化空间。
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTE3MDI0MTU2MjIsNTUxMDI3NzE3LC0yMj
YyNDM2MjMsMjYxMjUxMDkzLDI0OTI3MTcyNyw0MDIzNTk5Nzcs
LTY4ODk3NTkxNSwtNDA1NDQ0MjIwLC0xOTQ2MTYyNzgwLC0zOT
g3OTUyNzMsMjA4NDk2NjQ3MV19
-->