# SQL慢查询排障优化记录

## 1. 建表，导入数据。

表结构：
```
CREATE TABLE `tb_sku` (
  `id` int(11) NOT NULL AUTO_INCREMENT COMMENT '商品id',
  `sn` varchar(100) NOT NULL COMMENT '商品条码',
  `name` varchar(200) NOT NULL COMMENT 'SKU名称',
  `price` int(20) NOT NULL COMMENT '价格（分）',
  `num` int(10) NOT NULL COMMENT '库存数量',
  `alert_num` int(11) DEFAULT NULL COMMENT '库存预警数量',
  `image` varchar(200) DEFAULT NULL COMMENT '商品图片',
  `images` varchar(2000) DEFAULT NULL COMMENT '商品图片列表',
  `weight` int(11) DEFAULT NULL COMMENT '重量（克）',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  `category_name` varchar(200) DEFAULT NULL COMMENT '类目名称',
  `brand_name` varchar(100) DEFAULT NULL COMMENT '品牌名称',
  `spec` varchar(200) DEFAULT NULL COMMENT '规格',
  `sale_num` int(11) DEFAULT '0' COMMENT '销量',
  `comment_num` int(11) DEFAULT '0' COMMENT '评论数',
  `status` char(1) DEFAULT '1' COMMENT '商品状态 1-正常，2-下架，3-删除',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```
登录客户端
```
mysql --local-infile -u root -p
```
导入数据
```
set global local_infile=1;
load data local infile '/home/jonathanai/test/tb_sku1.sql' into table `tb_sku` fields terminated by ',' lines terminated by '\n';
```
追踪慢查询日志
```
tail -f /var/lib/mysql/192-slow.log
```

## 2. 查询数据，追踪慢查询
慢查询语句1（经日志捕捉到执行时间为1.07s)：查询指定品牌、品类的上架商品的ID、名字和销量

```
SELECT id,name,sale_num FROM tb_sku 
WHERE category_name = '牛仔裤' AND status = '1' AND brand_name='ME&CITY' 
ORDER BY id;

```

优化办法：建立包含category_name, status, brand_name的联合索引。
```
create index idx_sku_cate_sta_bra 
on tb_sku(category_name,status,brand_name);
```
字段|优化前（无联合索引）|优化后（建立联合索引）|性能解读
--|--|--|--
select_type|SIMPLE|SIMPLE|单表普通查询，无关联子查询，无变化tabletb_skutb_sku操作同一张商品表typeindexref巨大提升

优化前 type=index：遍历整棵主键索引树（全索引扫描，接近全表）

优化后 type=ref：等值匹配索引，精准定位数据，性能远优于 indexpossible_keysNULLidx_sku_cate_sta_bra优化前无可用索引；优化后匹配自定义联合索引keyPRIMARYidx_sku_cate_sta_bra优化前只能走主键全索引遍历；优化后命中业务筛选联合索引key_len41211key_len 代表索引占用字节，多字段联合索引长度更长，可覆盖多个筛选条件refNULLconst,const,const优化前无常量匹配；优化后 3 个查询条件全部是常量等值匹配，索引精准命中rows18020832029扫描行数暴跌

优化前需要扫描全表 180w 行；优化后仅扫描 2029 行，IO 读取量大幅减少filtered0.10100.00优化前过滤率仅 0.1%，扫描百万行仅留下千条有效数据；

优化后过滤率 100%，索引直接筛选出全部符合条件数据，无无效扫描ExtraUsing whereUsing index condition1. 优化前 Using where：先读取主键完整行，MySQL 服务层再过滤条件，大量回表 IO

2. 优化后 Using index condition：开启 ICP 索引下推，存储引擎层提前过滤索引字段，减少回表次数

慢查询日志捕捉信息：
![慢查询日志](/imgs/2026-07-24/q9EW4dCyvmW4PKtj.png)
优化前后explain对比：
![输入图片说明](/imgs/2026-07-24/xCmEn2pgBZOKQBOz.png)
![输入图片说明](/imgs/2026-07-24/Pj93Fq6lJBz6Vann.png)
#
<!--stackedit_data:
eyJoaXN0b3J5IjpbOTA0NzcxOTA3LDIwODQ5NjY0NzFdfQ==
-->